type User {
  id: ID!
  email: String!
  name: String
  password: String
  posts: [Post!]!
}

# type Post {
#   id: ID!
#   title: String!
#   content: String!
#   published: Boolean!
# }

type Query {
  getOneUser(id: ID!): User
}